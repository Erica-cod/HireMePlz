apiVersion: v1
kind: ResourceQuota
metadata:
  name: hiremeplz-quota
  namespace: hiremeplz
  labels:
    app.kubernetes.io/part-of: hiremeplz
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi
    pods: "20"
    persistentvolumeclaims: "5"
