apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: hiremeplz
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: hiremeplz
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - port: 4000
      targetPort: http
      name: http
