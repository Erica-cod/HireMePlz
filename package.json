{
  "name": "hiremeplz",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend",
    "extension",
    "worker",
    "worker-llm"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:worker": "npm run dev --workspace worker",
    "dev:worker-llm": "npm run dev --workspace worker-llm",
    "build": "npm run build --workspace backend && npm run build --workspace frontend && npm run build --workspace extension && npm run build --workspace worker && npm run build --workspace worker-llm",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend && npm run lint --workspace extension && npm run lint --workspace worker && npm run lint --workspace worker-llm",
    "db:generate": "npm run db:generate --workspace backend",
    "db:push": "npm run db:push --workspace backend"
  }
}
